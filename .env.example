# Auth0/Okta Configuration
OKTA_ISSUER_URI=https://your-domain.auth0.com/
OKTA_CLIENT_ID=your-client-id
OKTA_CLIENT_SECRET=your-client-secret

# Application Configuration
SECRET=your-session-secret
BASE_URL=http://localhost:3000
REDIRECT_URI=http://localhost:3000/dashboard
PORT=3000

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual Auth0/Okta credentials
# 3. For production (Render), update BASE_URL and REDIRECT_URI with your app's URL
# 4. Never commit .env to version control - it's already in .gitignore